<div class="flex-between pad-btt-sm cap-width separate-above">
	<h2 style="margin: 0;">From the blog</h2>
	<a href="/blog/">See all blog posts</a>
</div>
	<ul class="pad-top-lg pad-btt-md no-list-style image-scroll">
		{%- for post in collections.blog | reverse | limit(3) -%}
		<li class="cap-width-sm card ratio-1-1">
			<article>
				<h3 style="margin:0;"><a href="{{ post.url | url }}">{{ post.data.title }}</a></h3>
				{% if post.data.teaser %}<small class="pad-top-sm">{{ post.data.teaser }}</small>
				<small class="pad-top-sm light">{{ post.date | readableDate }}</small>
				{% endif %}
			</article>
		</li>
		{%- endfor -%}
	</ul>