<!DOCTYPE html>
<html lang="en">
{% include "_head.njk" %}

<body>
	<header>
		{%- include "_header.njk" -%}
	</header>

	<main class="page-margins pad-btt-md">
			<div class="secondary-header pad-btt-md">
				<div>
					<h1>{{ navtitle | default(title) }}</h1>
					<div>
						{% if date %}
						<small class="light pad-top-sm">Francesco Imola</small>
						<small class="light">{{ date | readableDate }}</small>
						{% endif %}
						{% if category %}
						<small class="light">Tagged:&nbsp;
							{%- for category in collections.blogCategories -%}
							<a href="/blog/{{ category | slugify }}/">{{ category }}</a> {% if not loop.last %} | {% endif %}
							{%- endfor -%}
						</small>
						<small><a href="/blog/">See all posts</a></small>
						{% endif %}
					</div>
				</div>
			</div>
			{% if image %}
			<img src="{{ image }}" width="645" height="323" {% if imageAlt %}alt="{{ imageAlt }}"{% else %}alt=""{% endif %} loading="lazy" decoding="async" eleventy:ignore />
			{% endif %}
			<section class="pad-top-lg cap-width post-content">
				{{ content | safe }}
				{% include "_social-share.njk" %}
			</section>
	</main>

	{%- include "_footer.njk" -%}

	<script src="/assets/app-core.js" defer></script>
</body>

</html>