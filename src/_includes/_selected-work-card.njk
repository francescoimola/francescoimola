{%- macro selectedWorkCard(heading, linkUrl = '/portfolio/', image = {}) -%}
<section class="pad-btt-md cap-width selected-work-card rounded">
	<a href="{{ linkUrl | default('/portfolio/') }}">
		<img src="{{ image.src | default('https://res.cloudinary.com/dax6x2pto/image/upload/c_lfill,w_1290,f_auto/dpr_auto/ezgif.com-optipng_pao87g.png') }}"
				width="{{ image.width | default('1400') }}" height="{{ image.height | default('787') }}" alt="{{ image.alt | default('') }}" fetchpriority="{{ image.fetchpriority | default('high') }}"
				decoding="async">
		<div class="flex-between"><h2>{{ heading }}</h2><arrow-right-icon>→</arrow-right-icon></div>
	</a>
</section>
{%- endmacro -%}
